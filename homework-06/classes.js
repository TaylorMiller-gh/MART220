class keyBtn {

  constructor (audio, x, y, w, h, mainClr, scndClr, ) {
    this.audio = audio;
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
  }

  getAudio() {
    return this.audio;
  }

  getX() {
    return this.x;
  }

  getY() {
    return this.y;
  }

  getW() {
    return this.w;
  }

  getH() {
    return this.h;
  }

}
